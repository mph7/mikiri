# MongoDB Connection String
# Replace <DB_USERNAME>, <DB_PASSWORD>, and your_cluster_url.mongodb.net with your actual credentials and cluster URL.
# Example: mongodb+srv://myuser:mypassword@mycluster.xyz.mongodb.net/mikiri?retryWrites=true&w=majority
DB_CONNECTION_STRING="mongodb+srv://<DB_USERNAME>:<DB_PASSWORD>@your_cluster_url.mongodb.net/mikiri_db_name?retryWrites=true&w=majority"

# JWT Secret for signing tokens - use a strong, random string
JWT_SECRET="your-very-strong-and-random-secret-key"

# Port the backend server will run on
PORT=4000